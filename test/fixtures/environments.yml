# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  application: one
  deployment_target: one
  name: development
  default: true

two:
  application: two
  deployment_target: one
  name: staging
  default: false
