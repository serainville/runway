# SQLite is used for local development and test so MySQL is not required locally.
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: storage/runway_development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: storage/runway_test.sqlite3

# MySQL is used for staging and production.
mysql_default: &mysql_default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: <%= ENV.fetch("DB_USERNAME") { "root" } %>
  password: <%= ENV["DB_PASSWORD"] %>
  host: <%= ENV.fetch("DB_HOST") { "127.0.0.1" } %>

staging:
  primary:
    <<: *mysql_default
    database: runway_staging
  cache:
    <<: *mysql_default
    database: runway_staging_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *mysql_default
    database: runway_staging_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *mysql_default
    database: runway_staging_cable
    migrations_paths: db/cable_migrate

# As with config/credentials.yml, you never want to store sensitive information,
# like your database password, in your source code. If your source code is
# ever seen by anyone, they now have access to your database.
#
# Instead, provide the password or a full connection URL as an environment
# variable when you boot the app. For example:
#
#   DATABASE_URL="mysql2://myuser:mypass@localhost/somedatabase"
#
# If the connection URL is provided in the special DATABASE_URL environment
# variable, Rails will automatically merge its configuration values on top of
# the values provided in this file. Alternatively, you can specify a connection
# URL environment variable explicitly:
#
#   production:
#     url: <%= ENV["MY_APP_DATABASE_URL"] %>
#
# Read https://guides.rubyonrails.org/configuring.html#configuring-a-database
# for a full overview on how database connection configuration can be specified.
#
production:
  primary: &primary_production
    <<: *mysql_default
    database: runway_production
    username: <%= ENV.fetch("DB_USERNAME") { "runway" } %>
    password: <%= ENV.fetch("RUNWAY_DATABASE_PASSWORD", ENV["DB_PASSWORD"]) %>
  cache:
    <<: *primary_production
    database: runway_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: runway_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: runway_production_cable
    migrations_paths: db/cable_migrate
